import 'package:flutter/material.dart';

/// Bazar app color palette from Figma design
class AppColors {
  AppColors._();

  /// Deep purple - primary brand color (splash, buttons)
  static const Color primary = Color(0xFF5F2B9F);

  /// Muted purple/grey for secondary text
  static const Color secondaryText = Color(0xFF6B6B7B);

  /// Light grey for body text
  static const Color bodyText = Color(0xFF8E8E9E);

  /// White background for onboarding
  static const Color background = Color(0xFFFFFFFF);

  /// Dark text for titles
  static const Color titleText = Color(0xFF2D2D3A);
}
